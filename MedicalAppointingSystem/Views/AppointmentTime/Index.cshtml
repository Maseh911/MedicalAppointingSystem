@model IEnumerable<MedicalAppointingSystem.Models.AppointmentTime>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AppointedTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Patients)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AppointedTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Patients.FirstName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AppointedId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AppointedId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AppointedId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
